import { CanvasPreview } from "@/components/marketing/canvas-preview";

/** @deprecated Prefer CanvasPreview — kept for backwards-compatible imports. */
export function BuilderPreview() {
  return <CanvasPreview />;
}
