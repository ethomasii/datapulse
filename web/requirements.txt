# Co-located managed compute (Vercel Python). Kept lean — dbt runs via control-plane executor when needed.
fastapi>=0.115.0
httpx>=0.27.0
dlt>=1.5.0
psutil>=6.0.0
mcp>=1.0.0
litellm>=1.50.0
pandas>=2.0.0
sqlalchemy>=2.0.0
