# GitHub Actions

Starter workflows for:

- Pinning `ghcr.io/eltpulsehq/agent` by digest
- Injecting `ELTPULSE_AGENT_TOKEN` from GitHub Actions secrets
- Optional deploy to ECS/Kubernetes via your existing patterns

Add `*.yml` samples here; keep secrets out of the repo.
