# GitOps

Sample **Flux** or **Argo CD** manifests for deploying the gateway `Deployment` with sealed secrets or external secrets operator.

Same agent image and env contract as Docker; only the delivery mechanism changes.
