# Libraries

Optional small packages (Python, TypeScript, Go) that wrap `/api/agent/*` or help CI—not required to run the gateway container.

Add a subdirectory per package with its own README and install instructions (Git `subdirectory=` installs until anything is published to PyPI/npm).
