# Examples

Minimal runnable setups for the eltPulse gateway next to common runtimes.

| Example | Status |
|---------|--------|
| [docker](docker/) | Starter: `docker run` with `ELTPULSE_*` env vars |

Contributions: add Dagster/Airflow/Prefect sidecar patterns here with a short README each.
