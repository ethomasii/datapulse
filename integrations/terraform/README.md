# Terraform

Modules and root examples for:

- ECS/Fargate task + service + secrets
- EKS/Kubernetes-facing resources (IRSA, security groups egress-only)

Keep modules small and opinionated; link from recipes for full stacks.
