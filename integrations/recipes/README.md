# Recipes

Short how-tos (markdown only):

- Hybrid account: some pipelines eltPulse-managed, some customer gateway
- Org default gateway (Clerk org workspace)
- CI triggering runs via session API or incoming webhook token

Add one file per recipe; link from the root README table when stable.
