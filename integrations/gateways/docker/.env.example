# Copy to .env (never commit .env)
ELTPULSE_AGENT_TOKEN=
ELTPULSE_CONTROL_PLANE_URL=https://app.eltpulse.dev

# Executor is set in docker-compose.yml (ELTPULSE_RUNNER=docker).
# Add any connection secret env vars here if not using the Connections page:
# GITHUB_TOKEN=
# SNOWFLAKE_ACCOUNT=
