# Webhook starters

Patterns for **inbound** HTTP triggers that eltPulse exposes (e.g. pipeline run webhooks): verify signatures, idempotency keys, and safe retries.

Not to be confused with the **gateway agent**, which **polls** the control plane.
