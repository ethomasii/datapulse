# OpenAPI

Machine-readable API contracts for **`/api/agent/*`** may be published here or linked from [`eltpulsehq/agent`](https://github.com/eltpulsehq/agent) / the app repo.

Use this folder for `openapi.yaml` snapshots and codegen notes when available.
