{
  "name": "eltpulse-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal eltPulse gateway — polls manifest/runs, posts heartbeats. Optional stub run completion for demos.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.758.0",
    "@aws-sdk/client-sqs": "^3.758.0"
  }
}
